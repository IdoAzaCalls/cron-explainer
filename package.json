{
  "name": "cron-explainer",
  "version": "0.1.2",
  "private": true,
  "description": "Pay-per-call Cron Expression Explainer API — Cloudflare Workers runtime.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/IdoAzaCalls/cron-explainer.git"
  },
  "homepage": "https://cron-explainer.cron-explainer-prod.workers.dev/",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "croner": "^8",
    "cronstrue": "^2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^1",
    "wrangler": "^3"
  },
  "engines": {
    "node": ">=20"
  }
}
