{
  "name": "cron-explainer",
  "version": "0.1.0",
  "private": true,
  "description": "Pay-per-call Cron Expression Explainer API — Cloudflare Workers runtime.",
  "license": "SEE LICENSE IN docs/tos.md",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "croner": "^8",
    "cronstrue": "^2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "^5",
    "vitest": "^1",
    "wrangler": "^3"
  },
  "engines": {
    "node": ">=20"
  }
}
